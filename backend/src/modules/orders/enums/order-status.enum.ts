export enum OrderStatus {
  received = 'received',
  diagnosis = 'diagnosis',
  waiting_approval = 'waiting_approval',
  in_progress = 'in_progress',
  testing = 'testing',
  ready = 'ready',
  delivered = 'delivered',
  cancelled = 'cancelled',
  rejected = 'rejected',
}
